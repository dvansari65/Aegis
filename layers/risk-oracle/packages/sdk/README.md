# SDK Package

Integration SDK for external protocols and clients.

Planned packages:
- TypeScript SDK for bots, dashboards, and backend services
- Rust client helpers for Solana-side integrations

The SDK should make risk reads simple and stable for integrators.

